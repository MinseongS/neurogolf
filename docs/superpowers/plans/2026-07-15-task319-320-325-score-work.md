# Task319/320/325 Score Improvement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convert every concrete score opportunity from the task301..325 discovery into a source-owned, gated, adopted deployment improvement.

**Architecture:** Work in three isolated task directories.  For task319 and task320, beat the stale manifest cost by at least one byte so normal `ng gate` remains authoritative without editing the manifest.  For task325, remove the redundant diagonal carrier first and attempt the shift-parameter rewrite only behind an analytical cost test.

**Tech Stack:** Python 3.13 via `uv`, ONNX 1.21.0, ONNX Runtime 1.26.0, pytest, NeuroGolf `ng` CLI.

## Global Constraints

- Candidates and scratch files live only under `candidates/taskNNN/`.
- Do not edit deployed ONNX files directly; every adoption is `ng gate` then `ng adopt`.
- Preserve all unrelated dirty files from concurrent sessions.
- Bundled fail must remain zero. Fresh evaluation is diagnostic.
- Keep `onnx==1.21.0` and `onnxruntime==1.26.0` pinned.
- Do not run `ng verify --update` while concurrent sessions are mutating the shared board.

---

### Task 1: task319 selected-profile tail deduplication

**Files:**
- Create: `candidates/task319/test_tail_dedup.py`
- Create: `candidates/task319/build_tail_dedup.py`
- Modify: `src/custom/task319.py`
- Produce: `candidates/task319/selected_profiles_tail.onnx`

**Interfaces:**
- Consumes: `src.custom.task319.build(task)` and its `IDX1_NEW` Gather index.
- Produces: `build_candidate()` returning a source-owned ONNX model with cost at most 4329.

- [ ] **Step 1: Write the failing test**

```python
def test_candidate_reuses_existing_scalar_one_index():
    model = build_candidate()
    names = {item.name for item in model.graph.initializer}
    assert "IDX1_NEW" not in names
    assert gather_for("safe_name_45").input[1] == "safe_name_11"
    assert calculate_params(model) == 167
```

- [ ] **Step 2: Verify RED**

Run: `uv run pytest candidates/task319/test_tail_dedup.py -q`
Expected: import failure because `build_tail_dedup.py` does not exist.

- [ ] **Step 3: Implement the minimal graph rewrite**

```python
model = build(None)
for node in model.graph.node:
    if node.op_type == "Gather" and node.input[1] == "IDX1_NEW":
        node.input[1] = "safe_name_11"
drop_initializer(model, "IDX1_NEW")
```

- [ ] **Step 4: Verify GREEN and bundled equivalence**

Run: `uv run pytest candidates/task319/test_tail_dedup.py -q`
Expected: tests pass and isolated candidate evaluation reports 267 pass, 0 fail, cost <=4329.

- [ ] **Step 5: Gate and adopt**

Run: `uv run ng gate candidates/task319/selected_profiles_tail.onnx --task 319`, then `uv run ng adopt ...` only on PASS.

---

### Task 2: task320 lost basis recovery and one-byte tail

**Files:**
- Create: `candidates/task320/test_basis_recovery.py`
- Create or modify: `candidates/task320/build_basis_recovery.py`
- Modify: `src/custom/task320.py`
- Produce: `candidates/task320/basis_recovery_tail.onnx`

**Interfaces:**
- Consumes: the cost-709 deployed polynomial graph and any recoverable cost-617 historical artifact.
- Produces: a source-owned exact graph with cost at most 616 so stale manifest cost617 can be beaten normally.

- [ ] **Step 1: Search historical artifacts before rederiving**

```bash
find submission/.backups /Users/minseong/.codex/worktrees -path '*task320*' -type f
```

- [ ] **Step 2: Write the failing structural and oracle test**

```python
def test_recovered_basis_beats_stale_manifest_and_matches_control():
    candidate = build_candidate()
    assert static_cost(candidate) <= 616
    assert bundled_failures(candidate) == []
```

- [ ] **Step 3: Verify RED**

Run: `uv run pytest candidates/task320/test_basis_recovery.py -q`
Expected: builder missing or cost remains 709/617.

- [ ] **Step 4: Recover or rederive the 709→669→633→617 factorization**

```python
# Preserve the free-output Einsum and rewrite only its dense polynomial operands.
# Validate pre-threshold scores exhaustively for lengths 0..10 and coordinates 0..29.
```

- [ ] **Step 5: Remove at least one duplicate scalar initializer or equivalent tail byte**

Run the initializer consumer/dedup inventory, rewire a shape-compatible duplicate, and require final cost <=616.

- [ ] **Step 6: Verify, gate, and adopt**

Run the task test, isolated score, `ng gate`, and `ng adopt` only after PASS.

---

### Task 3: task325 diagonal carrier removal

**Files:**
- Create: `candidates/task325/test_diag_elision.py`
- Create: `candidates/task325/build_diag_elision.py`
- Modify: `src/custom/task325.py`
- Produce: `candidates/task325/diag_elision.onnx`

**Interfaces:**
- Consumes: current cost-1234 signed fp16 output-fold graph.
- Produces: `build_candidate()` using `diagfull` directly and cost about 1198.

- [ ] **Step 1: Write the failing test**

```python
def test_diag_and_is_elided_without_changing_threshold_output():
    candidate = build_candidate()
    assert "diag" not in node_outputs(candidate)
    assert where_condition(candidate, "signed_code16") == "diagfull"
    assert bundled_failures(candidate) == []
```

- [ ] **Step 2: Verify RED**

Run: `uv run pytest candidates/task325/test_diag_elision.py -q`
Expected: import failure because the builder is absent.

- [ ] **Step 3: Implement minimal rewrite**

```python
remove_node_producing(model, "diag")
signed_where.input[0] = "diagfull"
```

- [ ] **Step 4: Verify GREEN, gate, and adopt**

Run pytest, isolated bundled evaluation, `ng gate`, then `ng adopt` only on PASS.

---

### Task 4: task325 shift-matrix feasibility probe

**Files:**
- Create: `candidates/task325/test_shift_probe.py`
- Create: `candidates/task325/build_shift_probe.py`

**Interfaces:**
- Consumes: current Euler scalar formula and its 900-element `S` parameter.
- Produces: a priced Eh-only toy or a measured pivot decision.

- [ ] **Step 1: Write a failing analytical test requiring net saving >=118**
- [ ] **Step 2: Verify RED against a naive Slice/Pad/Conv implementation**
- [ ] **Step 3: Implement only attribute/kernel variants that do not materialize a counted 30x30 plane**
- [ ] **Step 4: Expand to the full graph only if measured candidate cost can reach <=1116**
- [ ] **Step 5: Otherwise record the exact tool/date/verdict/reopen four-field ledger entry under the selected live lever**

---

### Task 5: integration verification and handoff

- [ ] Run all new and existing scoped pytest suites.
- [ ] Run isolated `ng score` for every adopted task.
- [ ] Confirm artifact SHA matches each manifest entry.
- [ ] Rerun scoped mechanism scanners and register any reusable mechanism or mark it task-local.
- [ ] Replace `state/STATE.md` with the current shared truth without discarding concurrent handoffs.
- [ ] Do not pack or submit until concurrent sessions quiesce and a complete 400/400 verification succeeds.
