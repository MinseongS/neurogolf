"""Task 203 — exact source reconstruction of current live graph.

Generated by reports/scripts/live_to_exact_source.py.
This owns the live ONNX graph in Python source; it is a control baseline,
not a semantic optimization.
"""
from onnx import TensorProto, helper
from ._exact import arr_b64, model, tensor


def build(task):
    inits = [
        tensor('four', arr_b64('k05VTVBZAQB2AHsnZGVzY3InOiAnPGY0JywgJ2ZvcnRyYW5fb3JkZXInOiBGYWxzZSwgJ3NoYXBlJzogKCksIH0gICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgIAoAAIBA')),
    ]
    nodes = [
        helper.make_node('ReduceSum', ['input'], ['cnt'], axes=[2, 3], keepdims=0),
        helper.make_node('ReduceMax', ['cnt'], ['maxc'], keepdims=1),
        helper.make_node('Add', ['maxc', 'four'], ['pair']),
        helper.make_node('Sub', ['pair', 'cnt'], ['target']),
        helper.make_node('Transpose', ['target'], ['target_col'], perm=[1, 0]),
        helper.make_node('Equal', ['target_col', 'cnt'], ['match_b']),
        helper.make_node('Cast', ['match_b'], ['match_u8'], to=2),
        helper.make_node('ArgMax', ['match_u8'], ['remap'], axis=1, keepdims=0),
        helper.make_node('Gather', ['input', 'remap'], ['output'], axis=1),
    ]
    return model('task203_live_exact', nodes, inits, output_dtype=1, opset=12)
