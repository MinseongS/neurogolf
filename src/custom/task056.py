"""Task 056 — exact source reconstruction of current live graph.

Generated by reports/scripts/live_to_exact_source.py.
This owns the live ONNX graph in Python source; it is a control baseline,
not a semantic optimization.
"""
from onnx import TensorProto, helper
from ._exact import arr_b64, model, tensor


def build(task):
    inits = [
    ]
    nodes = [
        helper.make_node('Slice', ['input'], ['b0_f'], axes=[1, 2, 3], ends=[1, 1, 1], starts=[0, 0, 0]),
        helper.make_node('Slice', ['input'], ['b2_f'], axes=[1, 2, 3], ends=[1, 1, 3], starts=[0, 0, 2]),
        helper.make_node('Greater', ['b2_f', 'b0_f'], ['h1']),
        helper.make_node('Greater', ['b0_f', 'b2_f'], ['h3']),
        helper.make_node('Equal', ['h1', 'h3'], ['equal_bits']),
        helper.make_node('Cast', ['b0_f'], ['b0_bool'], to=9),
        helper.make_node('Not', ['b0_bool'], ['not_b0']),
        helper.make_node('And', ['equal_bits', 'not_b0'], ['h2']),
        helper.make_node('And', ['equal_bits', 'b0_bool'], ['h6']),
        helper.make_node('And', ['h1', 'h3'], ['false_bool']),
        helper.make_node('Concat', ['h1', 'h2', 'h3', 'false_bool', 'false_bool', 'h6'], ['active_bool'], axis=1),
        helper.make_node('Cast', ['active_bool'], ['active_channels'], to=10),
        helper.make_node('Pad', ['active_channels'], ['output'], mode='constant', pads=[0, 1, 0, 0, 0, 3, 29, 29], value=0.0),
    ]
    value_infos = [
    ]
    return model('task056_live_exact', nodes, inits, output_dtype=10, opset=9, value_infos=value_infos)
