# Session 2026-07-12 (b) — wave3 rank-machine audit

## Start-of-session checks (all clean)
- 54596069 = LB 7316.30 (MAIN v9) confirmed UNCHANGED. Local manifest 7316.1764.
- Codex manifest (14:37) = 7315.7994 → 0.377 BEHIND ours. Only cheaper task = 005 (+0.001)
  which is the known fresh-dirty net we reverted. NOTHING TO ABSORB.
- Latest kaggle sub 54596591 (codex, 05:38) = 7315.92 < our 7316.30. Our best stands.
- poll_public_dumps: nothing new (75 kernels scanned).

## Wave3 audit dispatched
- 59-task un-ledgered pool (cost>2000, not floor/nobuild/won), split 4 ways to opus audit agents.
- Each classifies rank-machine foldability → BUILD-CANDIDATE shortlist w/ mechanism + savings.
- Unmarked top-cost 255 (already fp16-optimized) & 157 (combinatorial backtracking wall) = not fresh targets.

## Wave3 audit RESULT: 0 build-candidates (all 4 slices)
- Entire cost>2000 un-ledgered pool (59 tasks) = wall-residue or already-folded to floor.
- NEW WALL PATTERN: data-dependent colour→channel routing (383/358/132) — separable geometry
  can't fold when output colour is input-derived (fixed-channel out>0 decode can't scatter).
- Recorded in memory neurogolf-worklist-is-wall-residue (mid-cost tier now also exhausted).
- Rank-machine 0.X engine = harvested across ALL cost tiers. Forward = exogenous (new dumps) only.

## Cheap orthogonal lanes checked
- kernel_collapse scan: 0 items (params lane dry).
- 182 "deploy gap": stale-log artifact, deployed already 16.289 (>> log's 14.79). No action.
- dtype_overpay scan: 168 flagged tensors. Top headline hits (070/199/177) = ledgered co-bind DEAD.
  Re-ranked by producer-bound; clean candidates (no Einsum/Mul/MatMul co-bind) = 054/096/148/170/208/359/396.
  0.X-scale claims: 310(+0.94)/359(+0.64)/170(+0.61). Dispatched recast+verify probe on 310/359/170
  (bit-identical arbiter). NOTE: 0.X-directive deprioritizes 0.0X dtype-tail; only probing 0.X-scale.

## PARALLEL CODEX SESSION advanced board to 7318.05 local (discovered mid-session)
- STATE.md + manifest were rewritten by codex during my session: manifest 7316.18 → 7317.99.
- NEW mechanism family: signed_polynomial_sparse_edit_einsum (+1.815 local) — cracks DYNAMIC COLOUR
  ROUTING (the wall my rank-machine audit flagged). Direct wins 078/084/288/297/320, trims
  293/336/371/378/387. Registry state/insights.yaml, artifacts candidates/signed_poly_{wave,adopt}/.
- Codex global signed-poly scan (candidates/signed_poly_wave/global_scan/) = thorough; top candidates
  harvested; high-score OPEN tail (354/368/205/250/145) = tasks MY audit already declared walls.
  My gap hypothesis 383/358/132: codex scored 80/52/100 (below threshold) — colour is arbitrary input
  content not a low-degree predicate, so resists signed-poly for the SAME reason as rank-machine. NOT a gap.
- Pending subs: 54597601 (v10, 7317.99) + 54597684 (v10+367 tail, 7318.05). Both PENDING Kaggle score.

## GATE-ARBITER independent fresh-gate of all 11 codex adoptions (my gate = arbiter, caught 005 before)
- 078/084/288/320/297: fail=0 on 3000 fresh (297's STATE "1600 cap" was process-memory, clean at 3000).
- 293/336/371/378/387: fail=0 on 2500 fresh.
- 367 (v10+ tail): fail 4/2500 = 99.84% pass — far above bar (219=43%-fail keeps full points). No concern.
- => Pending 7318.05 submission verified clean by independent arbiter. No silent-zero risk.

## FRONTIER: both endogenous fold lanes (rank-machine + signed-poly) harvested by the two sessions.
Forward = (1) pending LB confirmation, (2) exogenous new dumps (poll empty now), (3) new detection-fold
primitive (measured-closed under onnx 1.21 op-types per neurogolf-detection-floor-costmodel-proof).

## ⚠️ CRITICAL: signed-poly wave REGRESSED LB −16.46 (root cause found)
- v10 (54597601, local 7317.99) scored LB 7299.84 = −16.46 vs MAIN v9 7316.30. v10+ (54597684)=7299.90.
- Only change v9→v10 = commit 7c9350b "optimize signed polynomial sparse edits". So the wave caused it.
- CULPRIT = task084 (codex bisected, rollback 54597964). ROOT CAUSE (I found): 084 signed-poly net
  derives geometry from size=sqrt(ReduceSum(input))=sqrt(H·W) → ASSUMES SQUARE grids. True rule
  (cristianoc) uses H,W independently. ALL 084 local examples square (train{3,7,15}²/test10²/arc-gen{3..8}²)
  → passes ng gate + fresh + real train+test, but Kaggle-zeros on non-square private-suite grids.
- LB math EXACT: 084 pre-wave 17.453 (cost1895)→0, other wave gains +0.993 held → −16.46. ✓
- Rollback 54597964 (reverts 084 only, keeps 9 wins+367, local 7317.23) PENDING → should recover ~7317.3
  = NEW RECORD over v9. Other retained signed-poly nets grader-safe (math confirms).
- NEW GATE (memory neurogolf-arcgen-distribution-blindspot-silentzero): arc-gen+bundled share blind spots;
  gate from-scratch rebuilds vs cristianoc rule on NON-SQUARE / out-of-range inputs. Red flag: Sqrt(ReduceSum(input)),
  single scalar used for both row&col extent. Bit-identical golf exempt.

## Extend-signed-poly-tail (user directive) status
- Scout {064,093,206,273,283}: 0 buildable (arbitrary content/counting/already mem0). Fresh lane dry.
- 378 builder running — MUST pass new non-square/cristianoc gate before adoption.

## BOARD-WIDE blind-spot audit (non-overlapping w/ codex 084 fix) — RESULT: only 084 at risk
- Method: scan all 400 deployed nets for shape-collapse proxy (Sqrt/Less(sq,area)/area→arith fed by
  area=ReduceSum(input,all-axes)) ∩ square-only local examples (train+test+arc-gen all H==W).
- 16 nets flagged on raw Sqrt/area signature. After discriminating (is proxy FED BY area? are all
  local examples square?): **084 = SOLE HIGH (084-class: area-fed dim proxy + square-only).**
  358 = area-fed but has non-square real examples (12×11,15×14) + fresh 2000/2000 fail=0 → SAFE.
  Other 14 = Sqrt not of area (legit) → low risk.
- Conclusion: no OTHER latent 084-class silent-zero on the board. Final submission clean of this class
  once 084 fixed (codex owns). Reusable detector = area-fed-proxy ∩ square-only (add to gate suite).

## SCORE-PUSH DIRECTION (user 2026-07-12): #1 hidden-25s + #2 walls (drop #3 dumps — mined out, none coming pre-deadline)
- Dumps confirmed mined out: full min-merge over ALL public_dumps = only task023 +0.07.
- #1 lens = PURE SPATIAL REARRANGEMENT (reflection/tiling/fractal/transpose/crop/quadrant): output copies
  input cells only, folds to free-output Einsum/Gather w/ tiny index vectors → toward 25pt. We hold 3 (067/179/241).
  Pool = 127 tasks (cost>150, rule<200chars). Dispatched: 083 dedicated (4-way mirror, cost 210→~0 = +5pt potential),
  scout A {029,004,162,237,388,381,112,045,199,232}, scout B {335,346,398,271,115,041,194,344,183,136}.
  ALL builds gated: bundled fail=0 + fresh 2500 + NON-SQUARE vs cristianoc (084-lesson gate).
- #2 = wall-cohort scoping (233/366/158/054/133/285/286) for any new-primitive/matrix-power fold — feasibility only.
- Adopt policy: only WINs passing bundled+fresh+non-square; then ng pack + submit (check kaggle subs first — codex parallel).

## #2 WALL-COHORT scoping RESULT: no lever (confirmed-wall x5, marginal x2)
- 233/366/158/054/285/286 all intrinsic: fp32 detection read (~3600B/label, HARD-CLOSED by onnx1.21 type
  system — mixed-T Conv/Einsum rejected at checker, not ORT gap) + irreducible reconstruction state.
- 286 = the only true iterative (BFS flood); matrix-power/fixed-point form ALREADY deployed (task002-style,
  state counted, fp16 underflows over 189 steps → fp32 mandatory). 285 floor proven by 4.0x dense alt.
- 233 MARGINAL: external existence claim (12-15k nets) but no mechanism in ~10 sessions. 133 +0.10 "policy
  toggle" (4-slot→3-slot) = drops insurance slot = SILENT-ZERO TRAP per 084 lesson → AVOID.
- Forward for walls = NEW detection primitive (sub-3600B label from fp32 one-hot) or external dump. Both exogenous.
- #1 scout A: 0 builds (all fill/overlay/detection). Awaiting scout B + 083 dedicated builder.

## NEW RECORD 7318.00 (codex correctly rebuilt 084) + my #1/#2 exhausted
- sub 54598518 = LB **7318.00** (local 7317.88): codex 084 "low-magnitude sign oracle" 1895->985
  non-square-safe (exhaustive171 + fresh5000 div0) — the correct recovery we diagnosed (~985 ≈ my ~953 sketch).
  +0.65 over the 084-revert rollback (54597964=7317.35), +1.70 over v9 7316.30. 084 saga RESOLVED.
- #1 PURE-REARRANGEMENT lens = EMPTY on current board:
  * 083 (4-way mirror) ALREADY at fold floor: free-output Einsum, mem=0, 210 = 30x(H+W) CP-rank minimum.
    Sub-floor (sparse-init R/C -> 14 params, +2.7) GRADER-REJECTED (strict-infer rank-0 + sanitize no-rename).
  * 194 (C4 rotation tiling) at floor 900 (fp32 read + uint8 hold); +2.6 sub-floor needs uint8-Einsum kernel (absent).
  * scouts A+B (20 tasks): 0 builds — rest are draw/detect/count/dedup/recolor, not value-agnostic bijections.
- #2 walls = no lever (see above). 
- DURABLE: hidden-25 lens funnels to grader constraints we can't change (no-sparse-init, no-uint8-Einsum-kernel,
  mixed-T Conv/Einsum rejection). +5.3 locked pts (083+194) PROVE the folds exist but are grader-blocked.
  Reopen ONLY if onnx strict-infer gains sparse-Einsum or ORT gains uint8-Einsum kernel.
- Board at endogenous floor 7318.00. Forward = new detection primitive (low-prob R&D) or exogenous dump (none coming).

## USER: "YOU set the record, don't wait for codex" — active build wave
- My tree still has BUGGY 084=833 (codex's 985 fix is in their submission only, not accessible in any worktree
  file — all codex worktree 084s test 1/6 non-square). Must un-break 084 myself.
- 023 dump "+0.07" = TRAP: bundled PASS but arc-gen fresh FAIL 266/3000=8.9% (overfit). My gate rejected it.
  (gate working — same catch as codex dirty 005.) Exogenous lane = nothing clean.
- 20260712_latest dumps (biohack44/evgendvorkin/etc, copied from codex 56ef): min-merge +0.0.
- LAUNCHED 3 builders (all hard-gated: bundled + fresh + NON-SQUARE vs cristianoc):
  * 084-correct: free-output overlay, extract H,W independently (W=max rowsum, H=area/W), target <985.
  * 388: 2×2 tessellation + cyan column-overlay hybrid (deployed 1829).
  * 112: anchored 4-quadrant red reflection about green centroid (deployed 1750).
- Plan: adopt clean wins → ng pack → submit (check kaggle subs first). Record = beat 7318.00.

## KEY: submission/overfit_nets/ is AUTHORITATIVE (ng pack bundles it), NOT networks/
- Submitted 084 = overfit_nets/task084.onnx = 8-node Sqrt(area) version, cost 833 = SQRT-BUGGY (Kaggle-zeros).
  networks/task084.onnx (9-node ScatterElements) is a DIFFERENT/stale variant. ng gate baseline = OVERFIT_NETS. good.
- So my submitted board's 084 contributes 0 to LB (silent-zero). Fixing it = the biggest single lever (~+18 LB,
  makes LB match local ~7317.9). A CORRECT 084 (~985 > 833) is a valid SILENT-ZERO REFIT — ng gate rejects it
  ("not strictly cheaper") so ADOPT MANUALLY (replace overfit_nets/task084.onnx, rebuild manifest, verify non-square).
- Fixing 084 → ~parity with codex 7318.00. To BEAT: need net-new win (112) OR cheaper-than-codex 084 (<985).
- 388 NO-BUILD (900B label plane = canonical floor for non-separable one-hot output). 023 dump dirty (rejected).
- Live hopes: 084-correct builder (competitive cost) + 112 builder (net-new).

## MY RECORD ATTEMPT SUBMITTED (built myself, not codex)
- My 084 minfix: replaced Sqrt(area) [square-assumption] with H=area/W (W=max rowsum). The 8-node
  free-output Einsum auto-bounds columns via input, so only the ROW scalar needed fixing. +1 ReduceSum only.
  cost 833(buggy,LB=0) -> 957 CORRECT (< codex's 985). Gates: bundled 175/175, arc-gen 0/3000,
  NON-SQUARE all pass within generator domain A<=21 (21x13 etc); >21 out-of-domain (both nets fail, moot).
  Adopted MANUALLY (silent-zero refit; ng gate rejects "not cheaper" vs buggy 833). Backup at candidates/task084/.
- Absorbed codex 367 exact-tail 13926->13126 (+0.06; fresh-gated: identical 2/3000 fail as mine = clean trim).
- Board = 7317.9118 local, 400/400 no zeros. codex record 54598518=7318.00 had local 7317.885 -> mine +0.027.
  Projected LB ~7318.03 (offset +0.115). SUBMITTED. Other builds 388/112 NO-BUILD (floor), 023 dump dirty(rejected).

## MY 084 RECORD ATTEMPT FAILED (sub 54599958 = 7299.89, silent-zero)
- minfix-084 (957, "correct") STILL Kaggle-zeroed. Root cause: my non-square gate tested ONLY H>=W grids.
  minfix FAILS every W>H (wider-than-tall) grid — private suite has W>H (and/or >21). codex's 985 handles them.
- LESSON (refines arcgen-blindspot memory): a non-square gate must test the FULL domain — H>=W AND W>H AND
  sizes beyond the generator range (gen caps A<=21 but private suite exceeds it), matching cristianoc EXACTLY
  incl. Python-wrap for W>H anti-diagonal. Incomplete gate = same silent-zero trap, twice.
- Reverted broken minfix-084 to prior state; kept clean 367 trim (13126). Record stays codex 7318.00 (54598518).
- STRATEGIC: endogenous board at floor (+0.03 ceiling, and even that needs a perfectly-correct net). Big points
  (+1..+665) require a NEW sub-3600B fp32->label detection primitive (~10 sessions unsuccessful; onnx1.21 type-lock).

## #2 DETECTION PRIMITIVE — empirically CLOSED (2026-07-12)
- Built + grader-measured label-producer test nets: fp32 [30,30] label = 3600B floor; uint8/bool variants
  pay the fp32 read FIRST (E2/E3 = 11721B), no sub-3600B path. Confirms the type-lock: any op reading fp32
  input outputs fp32; narrowing needs a Cast that counts on top. No primitive exists under onnx 1.21.
- Pivot: systematic cristianoc-rule-vs-deployed sweep over un-deep-audited mid-cost tasks (floors get falsified).
